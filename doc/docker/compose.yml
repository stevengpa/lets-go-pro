services:
  lets-go-pro:
    image: mysql:8
    container_name: lets-go-pro-db
    ports:
      - "3386:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root_pwd
      MYSQL_DATABASE: app_db
      MYSQL_USER: app_user
      MYSQL_PASSWORD: app_pwd
    volumes:
      - ./data:/var/lib/mysql
